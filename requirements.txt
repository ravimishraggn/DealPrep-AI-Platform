# --- Core API + ingestion (Phases 1-4) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic[email]==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
apscheduler==3.11.0
httpx==0.28.1
python-multipart==0.0.20

# --- Postgres (Phase 5-6): operational + structured (JSONB) + FTS (tsvector) ---
psycopg[binary]==3.2.3

# --- Extractors (Phase 5-6) ---
pdfplumber==0.11.4          # PDF text + table extraction

# --- Vector store (Phase 5-6) ---
# chromadb 1.x ships prebuilt wheels on Windows/py3.12 (0.5.x pinned a wheel-less
# chroma-hnswlib that needs a C++ compiler). Embeddings are supplied by us.
chromadb>=1.5,<2           # per-tenant vector collections
sentence-transformers==3.3.1  # local embeddings (all-MiniLM-L6-v2); pulls CPU torch

# --- Knowledge graph (Phase 5-6) ---
neo4j==5.27.0              # graph driver
spacy==3.8.2               # NER for EntityExtractor (needs: python -m spacy download en_core_web_sm)
anthropic==0.40.0          # RelationshipExtractor LLM client (optional; rule-based fallback if no key)

# --- Multi-agent orchestration (Phase 7) ---
langgraph>=0.2.14          # StateGraph, MemorySaver, interrupt_before (pulls langchain-core)
